version: 2

models:
  - name: int_user_repeat_orders
    description: Number of orders placed per customer over time
    # columns:
    #   - name: address_id
    #     data_tests:
    #       - not_null
    #       - unique